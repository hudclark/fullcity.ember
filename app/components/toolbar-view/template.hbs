	<div class="toolbar">
		{{#paper-button iconButton=true}}
			{{paper-icon icon='menu'}}
		{{/paper-button}}
		<span class="flex"></span>
		<div class="title">{{title}}</div>
		<span class="flex"></span>
		{{#paper-button iconButton=true}}
			{{paper-icon icon='search'}}
		{{/paper-button}}
	</div>

